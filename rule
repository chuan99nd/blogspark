- page config.yaml
    - main.md

file config.yaml follow theo

export interface PostMetadata {
    id: string;
    title: string;
    createdAt: string;
    modifiedAt: string;
    summary: string;
    tags: string[];
    catalog: string;
}

Todo:
- Add cover picture in post and blogCard, align blog content to center
- Add github workflow, validate and dd createdAt, modifiedAt 